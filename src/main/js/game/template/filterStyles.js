define(["com/pixijs/pixi", "com/pixijs/pixi-particles", "com/pixijs/pixi-filters"], function(PIXI) {    let filters = {		game0Objectives: new PIXI.filters.OutlineFilter(10, 0xFFFFFF),		resultPlaques: new PIXI.filters.OutlineFilter(10, 0xFFD89C),		prizeWin0: new PIXI.filters.OutlineFilter(4, 0xFF5D00),		prizeWin1: new PIXI.filters.OutlineFilter(4, 0xFFFFFF),		prizeNoWin: new PIXI.filters.OutlineFilter(6, 0xFFFFFF),		prizeShadow: new PIXI.filters.DropShadowFilter({rotation: 90, distance: 10, color: "black", alpha: 0.5, blur: 0}),		message0: new PIXI.filters.OutlineFilter(4, 0xFFEB01),		message1: new PIXI.filters.OutlineFilter(10, 0xFF0000),		messageGlow: new PIXI.filters.GlowFilter(100, 2, 0, 0xFFEB01, 0.1),		idleAll: new PIXI.filters.AdvancedBloomFilter()    };	filters.game0Objectives.padding = 10;	filters.resultPlaques.padding = 10;	filters.prizeWin0.padding = 20; //The inner outlines have to have enough padding for all the outlines together + the dropShadow	filters.prizeWin1.padding = 4;	filters.prizeNoWin.padding = 20; //inner outline (drop shadow)	filters.prizeShadow.padding = 10;	filters.message0.padding = 114; //inner outline	filters.message1.padding = 114; //inner outline	filters.messageGlow.padding = 114;	return filters;});